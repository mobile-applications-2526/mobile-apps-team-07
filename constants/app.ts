/**
 * App Constants
 * 
 * General application-wide constants.
 */

// App info
export const APP_NAME = 'Safarban';
export const APP_VERSION = '1.0.0';

// Animation durations (ms)
export const ANIMATION_DURATION = {
  fast: 150,
  normal: 250,
  slow: 400,
};

// Toast settings
export const TOAST_DURATION = 2500;

// Modal settings
export const MODAL_DISMISS_THRESHOLD = 100;

// List settings
export const DEFAULT_PAGE_SIZE = 20;
export const VESSEL_NAME_TRUNCATE_LENGTH = 25;
